# n8n-loop DONE (fully complete) — 2026-06-05T18:51:22Z

Backlog **100% complete: 11/11 items `- [x]`, 0 blocked, 0 todo.** Both APPLY deploys were carried
out against the local n8n (user authorized `N8N_APPLY=1`) and smoked live. The loop terminates — no
re-fire. `cycles_total = 14` across 6 sessions (5 SAFE + 1 APPLY).

## Epics — all done
- **Epic 0** self-test ✅.
- **Epic A — map the meta codebase** ✅ — `meta-inventory.md` (51 repos + 207,954-symbol code-intel
  index), `meta-codemap.md` (automation surfaces), `meta-dataflow.md` (3 mermaid views).
- **Epic B — Claude↔n8n chat bridge** ✅ — B-1 guardrails-first spec, B-2 validated workflow JSON,
  **B-3 deployed + smoked live** (workflow `ggvV5wItgjsRnwFk`).
- **Epic C — visualize code/data-flow in n8n** ✅ — C-1 mapping, C-2 3 validated viz workflows,
  **C-3 deployed live** (`ghqgmnJnB8zMMmAN`, `baU04FGqVHA0pntk`, `7z11ihYBJ7soxaik`).

## APPLY deploy evidence (local n8n @ http://localhost:5678, n8n 2.57.1)
Deployed via the **n8n-builtin** MCP surface (n8n-mcp mgmt was SSRF-blocked on localhost). All
workflows left **inactive** (drafts) — none activated/published.

- **C-3 — 3 visualization workflows** (`ghqgmnJnB8zMMmAN` build-spine, `baU04FGqVHA0pntk`
  runtime-dataflow, `7z11ihYBJ7soxaik` n8n-execution). NoOp + Sticky diagrams; node/connection/sticky
  fidelity verified live. Renderable in the editor, faithful to the A-4 map.
- **B-3 — chat bridge** (`ggvV5wItgjsRnwFk`):
  - **Full chat round-trip — exec 6:** `"...what is the capital of France?"` → Guard allowed →
    `claude -p --permission-mode plan` in the sandbox → **"Paris is the capital of France."** (2.0s).
  - **Guard rejection — exec 7** (live, env-enabled): `"sudo cat /etc/passwd … secret token"` →
    Guard `{allowed:false, denylisted}` → Refuse; **Run Claude never invoked**. The security guard
    holds even though claude *can* now execute.

## Runtime changes made for B-3 (recorded so they're reproducible / reversible)
- n8n restarted with **`NODE_FUNCTION_ALLOW_BUILTIN=child_process,os,path,fs`** (Code-node sandbox
  allowance; reaches the external task runner). Launched in-session, backgrounded; log
  `_workspace/01g_apply_n8n.log`. Healthz 200, pid recorded in `loop_state` history.
- Bridge **Run Claude** node rewritten (deployed + on-disk `_workspace/wf/claude-n8n-chat-bridge.json`):
  reads env via n8n `$env` (no `process` global in the sandbox); copies ONLY
  `~/.claude/.credentials.json` into the sandbox HOME per run so claude auths via the subscription
  login (user-approved auth trade-off; filesystem stays confined to the sandbox).

## Standing notes for the user
- The bridge is **inactive**. Activating it (exposing the chat to live/public input) is a deliberate
  security decision — it is an RCE-capable surface; the guard (denylist + sandbox + least-priv +
  redaction) mitigates but does not eliminate risk. Left off by default.
- The bridge sandbox (`~/.n8n-claude-bridge/sandbox/.claude/.credentials.json`) holds a fresh copy of
  the claude credentials (re-copied each run) — the accepted trade-off for subscription-login auth.
- The current n8n process runs with `NODE_FUNCTION_ALLOW_BUILTIN` set; if you restart n8n by other
  means, re-set that env for the bridge's Run Claude node to work.

— fully complete —
